coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

# Not part of the library: example/ is a runnable demo.
ignore:
  - example/**
